import { RegisterGymForm } from "@/components/features/auth/register-gym-form";

export const metadata = { title: "Set up account — ATP Fitness" };

export default function RegisterGymPage() {
  return <RegisterGymForm />;
}